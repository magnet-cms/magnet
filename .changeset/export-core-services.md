---
"@magnet-cms/core": minor
---

Export `EmailService`, `SettingsService`, and `WebhookService` from the core package public API so they can be injected in user applications without reaching into internal module paths.
