---
title: Plugins
description: Extend Magnet functionality with plugins
---

# Plugins

Plugins extend Magnet functionality with additional features.

## Available Plugins

| Plugin | Package | Description |
|--------|---------|-------------|
| Content Builder | `@magnet-cms/plugin-content-builder` | Visual content builder |
| SEO | `@magnet-cms/plugin-seo` | SEO metadata management |

## Using Plugins

Register plugins in your MagnetModule configuration:

```ts
import { MagnetModule } from '@magnet-cms/core'
import { ContentBuilderPlugin } from '@magnet-cms/plugin-content-builder'
import { SeoPlugin } from '@magnet-cms/plugin-seo'

MagnetModule.forRoot({
  plugins: [
    new ContentBuilderPlugin(),
    new SeoPlugin({
      defaultTitle: 'My Site',
    }),
  ],
})
```

## Plugin Architecture

Plugins can:
- Add new schema decorators
- Register API endpoints
- Add admin UI components
- Extend content services

## Creating Custom Plugins

```ts
import { MagnetPlugin } from '@magnet-cms/core'

export class CustomPlugin implements MagnetPlugin {
  name = 'custom-plugin'

  onInit(magnet: MagnetService) {
    // Plugin initialization
  }

  getSchemas() {
    return [] // Additional schemas
  }

  getRoutes() {
    return [] // Additional routes
  }
}
```

<Cards>
  <Card title="Content Builder" href="/docs/plugins/content-builder" description="Visual content builder" />
  <Card title="SEO Plugin" href="/docs/plugins/seo" description="SEO metadata management" />
</Cards>
