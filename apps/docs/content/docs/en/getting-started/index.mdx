---
title: Getting Started
description: Get started with Magnet CMS framework
---

# Getting Started

This guide will help you get up and running with Magnet in minutes.

## Prerequisites

Before you begin, ensure you have:

- **Bun** 1.2.2 or later (recommended) or Node.js 18+
- **MongoDB** 6.0+ (for Mongoose adapter)
- A terminal and code editor

## Installation

<Steps>
  <Step>
    ### Create a new NestJS project

    ```bash
    bun create nest my-cms
    cd my-cms
    ```
  </Step>
  <Step>
    ### Install Magnet packages

    ```bash
    bun add @magnet/core @magnet/common @magnet/adapter-mongoose
    ```
  </Step>
  <Step>
    ### Configure MagnetModule

    Update your `app.module.ts`:

    ```ts
    import { Module } from '@nestjs/common'
    import { MagnetModule } from '@magnet/core'
    import { MongooseAdapter } from '@magnet/adapter-mongoose'

    @Module({
      imports: [
        MagnetModule.forRoot({
          adapter: new MongooseAdapter({
            uri: process.env.MONGODB_URI || 'mongodb://localhost:27017/magnet',
          }),
        }),
      ],
    })
    export class AppModule {}
    ```
  </Step>
  <Step>
    ### Start the development server

    ```bash
    bun run start:dev
    ```
  </Step>
</Steps>

## What's Next?

- [Installation Guide](/docs/getting-started/installation) - Detailed installation instructions
- [Quick Start](/docs/getting-started/quick-start) - Build your first schema
- [Configuration](/docs/getting-started/configuration) - Configure Magnet for your needs
