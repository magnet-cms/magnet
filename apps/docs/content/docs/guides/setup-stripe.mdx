---
title: Setup Stripe
description: Add Stripe payments and subscriptions to Magnet CMS with the Stripe plugin
---

# Setup Stripe

Add Stripe billing to Magnet CMS: products, prices, customers, subscriptions, Checkout, Customer Portal, and webhooks.

## Prerequisites

- A Stripe account with secret and webhook signing keys

## 1. Install

```bash
bun add @magnet-cms/plugin-stripe
```

## 2. Set Environment Variables

```bash
# .env
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PUBLISHABLE_KEY=pk_live_...   # Optional: for admin UI
```

## 3. Register

```typescript
import { Module } from '@nestjs/common'
import { MagnetModule } from '@magnet-cms/core'
import { MongooseDatabaseAdapter } from '@magnet-cms/adapter-db-mongoose'
import { StripePlugin } from '@magnet-cms/plugin-stripe'

@Module({
  imports: [
    MagnetModule.forRoot([
      MongooseDatabaseAdapter.forRoot(),
      StripePlugin.forRoot({ currency: 'usd' }),
    ]),
  ],
})
export class AppModule {}
```

## 4. Configure Stripe Webhook

In the Stripe Dashboard → Webhooks, create an endpoint pointing to your Magnet server. The plugin handles webhook verification and routing automatically.

## 5. Access the Admin UI

Once registered, the admin sidebar includes **Stripe Payments** with sections for customers, products, subscriptions, and payments.

## Related

- [Plugin System](/docs/concepts/plugin-system) — plugin architecture
- [Setup Polar](/docs/guides/setup-polar) — Polar.sh billing alternative
